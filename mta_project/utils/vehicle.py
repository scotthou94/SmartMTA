# Storing vehicle related data
class vehicle(object):
    # from VehiclePosition message
    currentStopNumber = None
    currentStopId = None
    timestamp = None

    # from VehicleStopStatus message
    currentStopStatus = None
